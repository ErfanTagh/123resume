/**
 * Shared resume palette — accent drives `linkColor` / section highlights.
 * All templates should use these defaults unless `styling` overrides them.
 */
/** True blue accent (kept for the "Blue" color-theme swatch). */
export const RESUME_ACCENT_BLUE = "#2563eb";
/** Red accent — the default resume accent across all templates. */
export const RESUME_ACCENT_RED = "#dc2626";
/** Default accent applied to headings/links when the user hasn't picked a color. */
export const RESUME_ACCENT_DEFAULT = RESUME_ACCENT_RED;
/** Gray-900 — primary name / title rail (LaTeX `titleColor`) */
export const RESUME_TITLE_GRAY = "#111827";
/** Gray-700 — body copy (LaTeX `textColor`) */
export const RESUME_BODY_GRAY = "#374151";
